# 主题二 · 方正 · 暗金 · 秩序感

> 深底 · 深墨 + 金色主色 · 直角 · 衬线宋体
> 气质：庄重、理性、克制、一锤定音
> 适用场景：储藏室 / 保管箱式的产品界面
> 本文件为**完整独立规范**：单拿本文件即可交付实施，不依赖任何既有产品与代码。
> 布局几何为全主题共享（第 6 节），与主题一 / 三完全一致。

---

## 1. 设计总纲

| 原则 | 含义 | 落地 |
|---|---|---|
| 全屏沉浸 | 内容全屏铺底，控件悬浮其上，不抢主视觉 | 遮罩 + 浮层结构 |
| 主题即语义 | 深墨背景 + 金色主色 = 秩序与分量 | 主色只用于关键操作 / 强调 |
| 克制但有戏剧性 | 排版克制，靠卡片 + 金线 + 渐变造庄重感 | 描边用弱金，高亮用亮金 |
| 内容优先 | 主角是内容本身，不是空壳 | 卡片语言统一，不堆装饰 |
| 统一组件语言 | 按钮 / 卡片 / 输入框 / 弹窗同套形状 | 全部直角 |

---

## 2. 色彩令牌

### 2.1 角色定义

| 角色 | 值 | 用途 |
|---|---|---|
| `background` | `#1C1B1E` | 整页背景（深墨） |
| `surface` | `#2D2A24` | 面板 / 侧栏 / 卡片外层 / 输入框底 |
| `surfaceAlt` | `#241F1A` | 更深一级强调底 |
| `primary` / `gold` | `#E0AE40` | 主色：主按钮填充 / 强调 / 图标 |
| `onPrimary` / `onGold` | `#241C07` | 主色底上的文字（深色，不用白字） |
| `textPrimary` | `#F2E9D6` | 正文（浅米白） |
| `textSecondary` | `#B09B74` | 次级文字 / 占位 / 弱化描边 |
| `keyOff` | `#6F6B60` | 钥匙未选中态图标（比次级文字更灰） |
| `cardBackground` | `#2D2A24` | 卡片底 |
| `cardTitle` | `#E0AE40` | 卡片标题（金） |
| `cardBody` | `#F2E9D6` | 卡片正文 |
| `cardMuted` | `#B09B74` | 卡片内说明文字 |
| `cardBorder` | `#E0AE40` 40% 不透明 | 卡片描边（金） |
| `chipBackground` | `#2D2A24` 92% 不透明 | 非高亮胶囊按钮底 |
| `chipForeground` | `#F2E9D6` 74% 不透明 | 非高亮胶囊按钮前景 |
| `chipBorder` | `#B09B74` | 非高亮胶囊按钮描边（弱金） |
| 输入框描边 | `#D8A84E` 1px | 输入框（亮弱金） |
| 按钮阴影 | 黑 16% | 胶囊按钮 |
| 卡片阴影 | 黑 6% | 卡片（层级靠描边，阴影克制） |

### 2.2 使用规则

- 颜色按「角色」使用，禁止在组件里写死色值。
- 主色按钮文字一律**深色**（不用白色）。
- 锁 / 钥匙开 = 金色图标，关 = `keyOff` 灰图标。

---

## 3. 字体

- 主字体：**思源宋体（Noto Serif SC）**——衬线宋体，方正、有分量。
- 回退字体：系统默认（PingFang SC / sans-serif）。
- **所有文字（含密文）一律用主体字体**，不切换等宽、不引入其它字体。
- 禁止自行拼装文字样式，一律引用下表组合样式。

| 档位 | 字号 / 字重 | 用途 |
|---|---|---|
| 字标 | 20 / 600 · 字距 4 | 品牌字标（金色） |
| 页面标题 | 20 / 600 | 顶栏 / 页面大标题（米白） |
| 卡片标题 | 16 / 600 | 卡片标题 |
| 正文 | 14 / 400 | 正文 |
| 辅助信息 | 12 / 400 | 说明 / 占位 |
| 密文样式 | 14 / 400 | 密文内容（仍用主体字体） |
| 按钮 | 14 / 600 | 所有按钮（主 / 危险 / 文字）字型一致 |

---

## 4. 形状 / 字重 / 阴影

| 令牌 | 值 |
|---|---|
| 圆角 | 全部 2（输入框 / 卡片 / 按钮 / 弹窗 / 门面） |
| 强字重 | 600 |
| 卡片阴影 | 黑 6%（克制；层级靠描边表达） |
| 按钮阴影 | 黑 16% |
| 输入框描边 | 弱金 1px；门面 / 高亮项可用亮金 |
| 切角 | 无（直角） |

不做夸张高亮动画，保持"判词感"。

---

## 5. 组件观感

| 组件 | 观感 |
|---|---|
| 胶囊按钮 · 高亮态 | 金色实底 + 深字 + 直角 |
| 胶囊按钮 · 普通态 | 面板底 + 弱金描边 |
| 主按钮 | 金色填充、深字、直角；常规高 48，门面等高场景 56 |
| 危险按钮 | 面板底 + 金线描边；**不用红色**，靠文案与确认步骤表达 |
| 弹窗内文字按钮 | 无底色、金色文字 |
| 输入框 / 检索框 | 面板底 + 1px 弱金描边 + 直角 |
| 条目行 | 左侧钥匙块（金 = 开 / 灰 = 关）+ 名称（金标题） |
| 门面 / 解锁 | 面板卡 + 金色描边 + 直角 + 轻阴影 |
| 顶部提示 | 面板卡 + 金线 + 顶部滑入横幅（滑入 240ms / 滑出 200ms），成功失败同款 |
| 空态 | 两行克制引文（纯中文，不用英文装饰） |
| 遮罩 | 背景色 4 档渐变（第 6.1 节）；侧栏面板色 3 档渐变（第 6.2 节） |

---

## 6. 布局几何（全主题共享，一字不差）

> 以下数值全部**以屏幕顶端 / 底端为基准**。"安全区" = 刘海 / 状态栏 / 底部手势条（各平台系统安全区）。手机 / 平板 / 桌面一致，不受渠道影响。

### 6.1 全屏页面模板（主页面 & 所有二级页共用）

| 令牌 | 值 | 含义 |
|---|---|---|
| 页面水平边距 `pageEdge` | 16 | 左 / 右 |
| 首条目距顶 `contentTopInset` | 140 | 自屏幕顶端，**固定**，不加安全区 |
| 内容底部留白 `contentBottomInset` | 236 | 自屏幕底端，**固定**，不加安全区 |
| 顶部遮罩高 `topScrimHeight` | 170 | 固定，不含安全区 |
| 底部遮罩高 `bottomScrimHeight` | 160 | 不含底部安全区；实际渲染 = 底部安全区 + 160 |
| 顶栏胶囊 / 标题距顶 `topChromeInset` | 8 | 安全区内再偏移 8 |

**顶部 / 底部遮罩渐变（4 档，底色 = 本主题背景色 `#1C1B1E`）：**

```
顶部（自上而下）：背景色 ×1.00 @ 0% → ×0.90 @ 34% → ×0.48 @ 72% → ×0.00 @ 100%
底部（自下而上）：同一串值镜像
```

**页面层级（自底向上）：**

```
1. 主体内容区：全屏、可滚动，首条距顶 140
2. 顶部遮罩：纯视觉（不拦截点击），高 170
3. 顶部浮层：安全区内放胶囊 / 标题 / 返回键，边距 16 / 8 / 16 / 0
4. 底部遮罩：纯视觉，高 = 底部安全区 + 160
5. 底部浮层：安全区内，压在底部遮罩之上
```

### 6.2 设置侧栏（左侧滑入面板）

| 令牌 | 值 | 含义 |
|---|---|---|
| 面板宽 | 屏宽 × 75% | 通栏满高，阴影从顶到底无缝 |
| 面板底色 | `surface` = `#2D2A24` | 右缘 1px 描边（`textSecondary` 45% 不透明）+ 右投影（黑 24% / 模糊 28 / 偏移 (8, 0)） |
| 内容顶部 `settingsTopInset` | 80 | = 状态栏 + 80 |
| 内容底部 `settingsBottomInset` | 150 | = 底部安全区 + 150 |
| 顶部遮罩高 `settingsTopScrim` | 150 | 与面板同色 |
| 底部遮罩高 `settingsBottomScrim` | 200 | 与面板同色 |

**侧栏顶部 / 底部遮罩渐变（与面板同色，3 档，不出现其它颜色色带）：**

```
顶部（自上而下）：面板色 ×1.00 @ 0% → ×0.92 @ 60% → ×0.00 @ 100%
底部（自下而上）：同一串值镜像
```

**侧栏浮层：**

- 顶部只有标题「设置」（安全区内 16 / 8 / 16 / 8，字号 20、加粗、主题文字色）；
  **无关闭按钮**，收起方式 = 点击遮罩 / 系统返回键。
- 面板**不整体套安全区**：遮罩与阴影从屏幕顶端整条延伸，内容自己让出安全区。

### 6.3 侧栏开合交互

| 令牌 | 值 |
|---|---|
| 展开：主页右移 | 主页左缘 → 屏宽 × 75%（仅右侧 1/4 可见） |
| 展开：面板滑入 | 面板左缘 −75% → 0 |
| 遮罩 | 盖住可见主页区，黑 26% 不透明 |
| 动画 | 340ms 缓出三次曲线（easeOutCubic） |
| 状态驱动 | **布尔标志即时翻转**：点击 → 立即更新 → 动画立即开始（无二次延迟）；开合之间用布尔防重入 |

> ⚠️ 踩坑提醒：不要用动画控制器倒计时去驱动"是否展开"的标志。关闭时该标志要到动画**结束**才翻转，面板会凭空多等一整段动画才开始回收（用户感知明显延迟）。必须布尔即时翻转 + 过渡动画。

### 6.4 动效时长

| 场景 | 值 |
|---|---|
| 侧栏滑入 / 滑出 | 340ms 缓出三次曲线（easeOutCubic） |
| 主动画 短 / 中 / 长 | 150 / 250 / 350ms |
| 标准曲线 | 缓入缓出三次曲线（easeInOutCubic）/ 缓出三次曲线 |
| 顶部提示横幅 | 滑入 240ms / 滑出 200ms |

---

## 7. 必守规则

1. 页面布局照抄第 6 节；皮肤照抄第 2–5 节；之外禁止裸数值。
2. 禁止硬编码颜色 / 间距 / 圆角 / 字号；颜色一律走角色令牌。
3. 动效时长与曲线只用 6.4 节。
4. 文档与实施冲突时，以本文档为准。
5. 需要新的几何数值时，先补进第 6 节再实施。