# 全局设计规范与开发思想

> 适用对象：本项目所有页面、组件，以及后续沿用这套体系的其他项目。
> 一句话：**全局规范优先，页面零定制；缺什么补全局，不就地解决。**

---

## 一、核心思想

### 1. 所有页面共用一个全局规范

- 页面不携带自己的"应变码"（临时样式、随手定义的按钮/颜色/间距）。
- 按钮、卡片、弹窗、输入框、间距、字号、颜色，全部来自全局主题令牌与全局组件库。
- 判定标准：页面代码里不出现裸色值、裸圆角、裸字号、复制粘贴后改参数的自定义组件。

### 2. 缺什么，就往全局加

- 页面需要新按钮、新样式、新组件时，不就地解决，而是先补进全局规范 / 全局组件库。
- 好处：以后任何页面都能直接复用，不会出现"同一个东西每个页面一个版本"。
- 全局规范因此会越来越完整，而不是每个页面各自膨胀。

### 3. 先定义，后使用

- 颜色按角色定义（背景、表面、主色、卡片、弱化文字、描边…），不在页面里直接写具体值。
- 组件按角色复用，不在页面里复制一份再改参数。

### 4. 结构比色值重要

- 真正可复用的是：令牌之间的角色关系、组件层级、交互节奏、内容呈现方式，而不是某些固定色值。
- 换一套主题只需改全局，所有页面自动跟随。

---

## 二、开发过程中的配套思想

5. **统一组件语言。** 弹窗、按钮、输入框、提示全项目统一，不混用系统原生控件。

6. **克制。** 阴影不浮夸；动效不炫技，只服务"哪里展开、哪里收起"；文案直接、稳定、不说教；排版靠间距和层级，不靠大字号堆。

7. **空态和错误态也是规范的一部分。** 空态要与整体风格一致；错误信息要清楚，并尽量告诉用户下一步怎么做。

8. **可读性是硬规则。** 任何主题下所有页面都清晰可读，文字层级稳定。

---

## 三、落地检查清单

- 做新页面：先翻全局组件库找现成的，没有才考虑加全局。
- Review 页面：有没有裸色值 / 随手样式 / "只此一处"的自定义？
- 加全局：按角色命名，同步更新设计规范文档。
- 通用组件放全局组件目录，不放页面私有目录。
- 一次性临时代码：用完即删，不进页面提交。
